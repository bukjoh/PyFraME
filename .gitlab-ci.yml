image: python:3.6

test:
    script:
        - pip install --upgrade pip
        - pip search numpy
        - pip search scipy
        - pip install numpy
        - pip install scipy
        - pip install nose
        - nosetests